<!DOCTYPE html>
<html lang ="ja">
    <head>
        <meta http-equiv="content-type"
         content="text/html; charset=UTF-8">
        <title><%=title %></title>
        <link rel="stylesheet" href='/stylesheets/style.css' />
        <link rel="stylesheet" href='/stylesheets/classic.css' />
    </head>
    <body>
        <main class="main d-flex w-100">
            <div class="container d-flex flex-column">
                <div class="row h-100">
                    <div class="col-sm-10 col-md-8 col-lg-6 mx-auto d-table h-100">
                        <div class="d-table-cell align-middle">
                            <div class="card">
                                <div class="card-body">
                                    <div class="mb-2"><%- content %></div>
                                    <div class="m-sm-4">
                                        <form method="POST" action="/users/<%= route %>">
                                            <div class="form-group">
                                                <label for="mail">ユーザー名</label>
                                                <input type="text" name="name" id="name" value="<%= form.name %>" class="form-control">
                                            </div>
                                            <div class="form-group">
                                                <label for="password">パスワード</label>
                                                <input type="text" name="password" id="password" value="<%= form.password %>" class="form-control">
                                            </div>
                                            <div class="text-center mt-4">
                                                <input type="submit" value="<%= btn_nm %>" class="btn btn-primary">
                                            </div>
                                        </form>
                                    </div>
                                    <p class="text-right mt-1 sign-link"><a href="/users/<%= link_to %>"><%= link_txt %>はこちら</a></p>
                                </div>
                            </div>     
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </body>
</html>